to compile install gcc
note i will not be embedding the extra needed files such as config.fish in this version using the c++ qrc option 

and use command 
g++ -std=c++23 -Wall -Wextra -o archconv+ main.cpp
